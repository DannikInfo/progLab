//
// Created by Данил Козловский on 2019-04-10.
//

#include "deque.h"

/*
template<class T>
void deque2<T>::addBack(T data){
    if(this->size() == getSize())
        removeFront();
    this->push_back(data);
}

template<class T>
void deque2<T>::addFront(T data){
    if(this->size() == getSize())
        removeBack();
    this->push_front(data);
}

template<class T>
void deque2<T>::removeFront(){
    this->remove(this->begin());
}

template<class T>
void deque2<T>::removeBack(){
    this->remove(this->end());
}

template<class T>
T deque2<T>::getBack(){
    T data = this->back();
    removeBack();
    return data;
}

template<class T>
T deque2<T>::getFront(){
    T data = this->front();
    removeFront();
    return data;
}

template<class T>
int deque2<T>::getSize(){
    return sizeLimit;
}



template<class T>
void deque2<T>::clear(){
    clear();
}
template<class T>
void deq<T>::sSize(int size){
    //sizeLimit = size;
}*/